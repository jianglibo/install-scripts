start:
  before start, you should use hadoop install script to create hbase folder in the hdfs.
  mkdir -p /user/hbase
  chown -R hbase /user/hbase
  After start, Hbase web ui should listen at port 16010
download-config:
  Download hbase configuration folder.
reconfig:
  to perform this action when adding new node.